import { PrimaryButton, PrimaryBtn } from "@acme/design-system";

export const MultilineUsageScreen = () => (
  <>
    <PrimaryButton
      onClick={() => {}}
    />
    <PrimaryBtn />
  </>
);

export const CustomCard = ({ children }: { children: React.ReactNode }) => <>{children}</>;
