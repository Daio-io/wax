import SwiftUI

struct DiscoverView: View {
    var body: some View {
        Tier {
            Button("Play") { }
            EpisodeCardView()
        }
    }
}

struct EpisodeCardView: View {
    var body: some View {
        Button("Play") { }
    }
}
